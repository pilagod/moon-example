id: 'project-frontend'
type: 'application'
language: 'typescript'

project:
  name: 'project-frontend'
  description: 'Frontend for project'

stack: 'frontend'

tasks:
  setup:
    command: 'npm install'

  test:
    command: 'npm run test'
    deps:
      - setup
