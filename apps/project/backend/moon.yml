id: 'project-backend'
type: 'application'
language: 'go'

project:
  name: 'project-backend'
  description: 'Backend for project'

stack: 'backend'

tasks:
  version:
    command: 'go version'

  test:
    command: 'go test ./...'
    deps:
      - version
